<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Configuration for Paradigm IFS
  ─────────────────────────────────────────────────
  Allows the Capacitor WebView to load map tiles from Google Maps
  tile CDN (mt0–mt3.google.com), Nominatim, and other HTTPS map
  providers. Without this, Android 9+ WebView blocks these requests
  and Leaflet shows a blank/dark map.
-->
<network-security-config>

    <!-- Trust the system CA store for all HTTPS traffic (default + explicit) -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Google Maps Tile CDN – required for Leaflet street/satellite/terrain tiles -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">google.com</domain>
        <domain includeSubdomains="true">googleapis.com</domain>
        <domain includeSubdomains="true">maps.googleapis.com</domain>
        <domain includeSubdomains="true">mt0.google.com</domain>
        <domain includeSubdomains="true">mt1.google.com</domain>
        <domain includeSubdomains="true">mt2.google.com</domain>
        <domain includeSubdomains="true">mt3.google.com</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- OpenStreetMap / Nominatim (reverse geocoding in popups) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">openstreetmap.org</domain>
        <domain includeSubdomains="true">nominatim.openstreetmap.org</domain>
        <domain includeSubdomains="true">tile.openstreetmap.org</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Supabase (API, auth, storage) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <domain includeSubdomains="true">supabase.in</domain>
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </domain-config>

    <!-- Local dev server – allow cleartext for localhost only -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>

</network-security-config>
