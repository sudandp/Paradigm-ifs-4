import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: '.env.local' }); import { reportGenerators } from './utils/reportGenerators'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const IST_OFFSET = 5.5 * 60 * 60 * 1000; async function run() { const d1 = new Date('2026-04-15T15:00:00Z'); const d2 = new Date('2026-04-16T15:00:00Z'); for (let d of [d1, d2]) { const nowIST = new Date(d.getTime() + IST_OFFSET); console.log('Testing', d.toISOString(), '...'); try { const data = await reportGenerators.attendance_daily(supabase, nowIST); console.log('Success!', data.totalPresent); } catch(e) { console.error('Failed!', e.message, e.stack); } } } run();
